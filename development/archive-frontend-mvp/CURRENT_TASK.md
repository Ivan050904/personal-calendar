# Current Task

Current wave: 16
Current task: done

## Status

Roadmap waves 0–16 are implemented for MVP scope.

Wave 16 readiness checklist:
- repository interfaces hide IndexedDB (`src/application/repositories.ts`);
- stable client UUIDs (`src/domain/ids.ts`);
- schema-versioned export/import (`src/application/backup.ts`, ADR-014);
- future API notes remain in `docs/10_API_FUTURE.md`;
- no server sync implemented (by design).

Do not start a new wave automatically. Stop and report.
