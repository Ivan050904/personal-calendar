# Local Setup

Architecture:
Browser → FastAPI → local MySQL.

Docker is not required.

Backend must provide `.env.example` with placeholders for DB and AI configuration.

Final agent report must contain exact copy-paste commands for:
1. backend dependencies;
2. `.env`;
3. MySQL database creation/configuration;
4. Alembic migrations;
5. backend startup;
6. frontend startup;
7. tests.

No manual table editing as normal setup.
